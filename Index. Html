<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>For My Bestie 🥺💗</title>
    <link href="https://fonts.googleapis.com/css2?family=Quicksand:wght@400;500;600;700&display=swap" rel="stylesheet">
    <style>
        :root {
            --pastel-pink: #ffd1dc;
            --pastel-lavender: #e6e6fa;
            --dark-pink: #d87093;
            --text-color: #5d4037;
            --white: #ffffff;
        }

        * {
            box-sizing: border-box;
            margin: 0;
            padding: 0;
            font-family: 'Quicksand', sans-serif;
        }

        body {
            background: linear-gradient(135deg, var(--pastel-pink), var(--pastel-lavender));
            color: var(--text-color);
            min-height: 100vh;
            display: flex;
            justify-content: center;
            align-items: center;
            overflow-x: hidden;
            text-align: center;
            padding: 20px;
        }

        .screen {
            display: none;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            width: 100%;
            max-width: 600px;
            animation: fadeIn 1s ease-in-out;
        }

        .screen.active {
            display: flex;
        }

        @keyframes fadeIn {
            from { opacity: 0; transform: translateY(20px); }
            to { opacity: 1; transform: translateY(0); }
        }

        h1, h2, h3 {
            color: var(--dark-pink);
            margin-bottom: 15px;
            line-height: 1.4;
        }

        p {
            font-size: 1.1rem;
            margin-bottom: 20px;
            line-height: 1.6;
            font-weight: 500;
        }

        .card {
            background: var(--white);
            padding: 30px;
            border-radius: 20px;
            box-shadow: 0 10px 25px rgba(0,0,0,0.05);
            margin-bottom: 20px;
            width: 100%;
        }

        button {
            background-color: var(--dark-pink);
            color: var(--white);
            border: none;
            padding: 12px 25px;
            font-size: 1.1rem;
            border-radius: 30px;
            cursor: pointer;
            font-weight: 600;
            transition: all 0.3s ease;
            box-shadow: 0 5px 15px rgba(216, 112, 147, 0.4);
            margin: 10px;
        }

        button:hover {
            transform: scale(1.05);
            box-shadow: 0 8px 20px rgba(216, 112, 147, 0.6);
        }

        /* Memory Polaroids */
        .memory-container {
            display: flex;
            flex-wrap: wrap;
            justify-content: center;
            gap: 15px;
            margin-bottom: 30px;
        }

        .polaroid {
            background: var(--white);
            padding: 10px 10px 15px 10px;
            border-radius: 5px;
            box-shadow: 0 5px 15px rgba(0,0,0,0.1);
            width: 140px;
            transform: rotate(-3deg);
            transition: transform 0.3s ease;
        }

        .polaroid:nth-child(even) {
            transform: rotate(4deg);
        }

        .polaroid:hover {
            transform: scale(1.1) rotate(0deg);
            z-index: 10;
        }

        .polaroid-img {
            width: 100%;
            height: 120px;
            background-color: #f0f0f0; /* Placeholder color */
            border-radius: 3px;
            margin-bottom: 10px;
            object-fit: cover;
        }

        .polaroid p {
            font-size: 0.85rem;
            margin: 0;
            color: var(--dark-pink);
            font-weight: 700;
        }

        /* Interactive Buttons Container */
        .action-buttons {
            display: flex;
            gap: 15px;
            justify-content: center;
            position: relative;
            min-height: 80px;
        }

        #noBtn {
            background-color: #a0a0a0;
            box-shadow: 0 5px 15px rgba(160, 160, 160, 0.4);
        }

        #noBtn.dodging {
            position: fixed;
            z-index: 100;
        }

        /* Animations */
        .sad-face {
            font-size: 4rem;
            margin: 10px 0;
            animation: bounce 2s infinite;
        }

        @keyframes bounce {
            0%, 100% { transform: translateY(0); }
            50% { transform: translateY(-10px); }
        }

        .heart-float {
            position: fixed;
            bottom: -10%;
            font-size: 1.5rem;
            animation: floatUp 4s ease-in-out forwards;
            z-index: 1000;
            opacity: 0.8;
        }

        @keyframes floatUp {
            0% { transform: translateY(0) scale(1); opacity: 1; }
            100% { transform: translateY(-120vh) scale(1.5); opacity: 0; }
        }

        /* Sparkles */
        .sparkle {
            color: #ffb6c1;
            font-size: 1.2rem;
            animation: twinkle 1.5s infinite alternate;
        }

        @keyframes twinkle {
            0% { opacity: 0.3; transform: scale(0.8); }
            100% { opacity: 1; transform: scale(1.2); }
        }
    </style>
</head>
<body>

    <!-- SCREEN 1: Intro -->
    <div id="screen1" class="screen active">
        <h1>Heyyy... ruk jao 🥺<span class="sparkle">✨</span>💗</h1>
        <button onclick="nextScreen(2)">Bas 2 minute meri baat sun lo 👉🥹</button>
    </div>

    <!-- SCREEN 2: Apology -->
    <div id="screen2" class="screen">
        <div class="card">
            <div class="sad-face">😔</div>
            <p>Okay... maan leta hoon. Mujhse galti hui hai.</p>
            <p>Shayad maine tumhe hurt kiya ya tumhe bura feel karaya... But trust me, mera intention kabhi tumhe hurt karna nahi tha 🥺💗</p>
            <p>Tum mere liye genuinely bohot special ho.</p>
            <button onclick="nextScreen(3)">I'm Really Sorry 🥺</button>
        </div>
    </div>

    <!-- SCREEN 3: Special Friendship -->
    <div id="screen3" class="screen">
        <div class="card">
            <h2 id="secretText">Waise ek baat bolun? 👀</h2>
            <button id="revealBtn" onclick="revealFriendship()">Haan bolo...</button>
            <div id="friendshipMsg" style="display: none; margin-top: 20px;">
                <p>Humari dosti mere liye normal nahi hai 💗</p>
                <p>Tum un logon mein se ho jinke bina din thoda incomplete sa lagta hai.</p>
                <p>Aur haan... tumse naraz rehna allowed hai, par mujhse zyada der tak naraz rehna bilkul allowed nahi 😤🥺</p>
                <button onclick="nextScreen(4)">Aage dekho 👉</button>
            </div>
        </div>
    </div>

    <!-- SCREEN 4: Memories & Forgiveness -->
    <div id="screen4" class="screen">
        <h2>Humari Yaadein 📸</h2>
        <div class="memory-container">
            <!-- REPLACE THE 'src' ATTRIBUTES WITH YOUR ACTUAL PHOTO URLS -->
            <div class="polaroid">
                <img src="https://via.placeholder.com/150/f9f9f9/d87093?text=Photo+1" alt="Pagalpanti" class="polaroid-img">
                <p>Humari Pagalpanti 🤭</p>
            </div>
            <div class="polaroid">
                <img src="https://via.placeholder.com/150/f9f9f9/d87093?text=Photo+2" alt="Late Night Talks" class="polaroid-img">
                <p>Humari Late Night Talks 🌙</p>
            </div>
            <div class="polaroid">
                <img src="https://via.placeholder.com/150/f9f9f9/d87093?text=Photo+3" alt="Special Bond" class="polaroid-img">
                <p>Humari Special Bond 💞</p>
            </div>
        </div>

        <div class="card" style="margin-top: 10px;">
            <h3 id="forgiveMsg">Ab batao... gussa khatam? 🥺👉👈</h3>
            <div class="action-buttons">
                <button onclick="nextScreen(5)">HAAN 😌💗</button>
                <button id="noBtn" onmouseover="dodgeButton()" onclick="dodgeButton()">Abhi bhi gussa 😤</button>
            </div>
        </div>
    </div>

    <!-- SCREEN 5: Success & Final Message -->
    <div id="screen5" class="screen">
        <div class="card">
            <h2>Yesss! 🥹💗</h2>
            <p>Meri best friend maan gayi! Ab ek cute si smile do 😊</p>
            <p>Aur haan... ab humari dosti aur bhi zyada strong 💞</p>
            <hr style="border: 0; border-top: 1px solid #ffd1dc; margin: 20px 0;">
            <p>Sorry meri special bestie 💗 Tum important ho... Tumhari smile important hai... Aur humari dosti bhi 💞 Ab smile karo na 😊🥺</p>
            <button onclick="showFinal()">Chalo ab smile karo 😚💗</button>
        </div>
    </div>

    <!-- SCREEN 6: Best Friends Forever -->
    <div id="screen6" class="screen">
        <h1 style="font-size: 2.5rem; margin-bottom: 10px;">Best Friends Forever? 🥹💞</h1>
        <div style="font-size: 5rem; margin-top: 20px;">👯‍♀️✨</div>
    </div>

    <script>
        // Smooth transitions between screens
        function nextScreen(screenNumber) {
            document.querySelectorAll('.screen').forEach(screen => {
                screen.classList.remove('active');
            });
            document.getElementById('screen' + screenNumber).classList.add('active');
        }

        // Reveal hidden message in Section 3
        function revealFriendship() {
            document.getElementById('revealBtn').style.display = 'none';
            document.getElementById('friendshipMsg').style.display = 'block';
            document.getElementById('friendshipMsg').style.animation = 'fadeIn 1s ease-in-out';
        }

        // Logic for the dodging "No" button
        let dodgeCount = 0;
        function dodgeButton() {
            const noBtn = document.getElementById('noBtn');
            const msg = document.getElementById('forgiveMsg');
            dodgeCount++;
            
            // Change text based on attempts
            if (dodgeCount === 1) {
                msg.innerText = "Areyyy please na 🥺 Itna bhi gussa mat ho... Main sach mein sorry hoon 💗";
            } else if (dodgeCount === 3) {
                msg.innerText = "Okay okay 😭 Last chance... please maan jao na meri bestie 🥺💞";
            }

            // Move the button randomly across the viewport
            noBtn.classList.add('dodging');
            
            // Calculate safe boundaries
            const maxX = window.innerWidth - noBtn.offsetWidth - 20;
            const maxY = window.innerHeight - noBtn.offsetHeight - 20;
            
            const randomX = Math.max(10, Math.floor(Math.random() * maxX));
            const randomY = Math.max(10, Math.floor(Math.random() * maxY));
            
            noBtn.style.left = randomX + 'px';
            noBtn.style.top = randomY + 'px';
        }

        // Final Heart Animations
        function showFinal() {
            nextScreen(6);
            createHeartsLoop();
        }

        function createHeartsLoop() {
            setInterval(() => {
                const heart = document.createElement('div');
                heart.innerHTML = ['💗', '💞', '✨', '🥺', '💖'][Math.floor(Math.random() * 5)];
                heart.classList.add('heart-float');
                
                // Randomize position, size, and speed
                heart.style.left = Math.random() * 100 + 'vw';
                heart.style.animationDuration = (Math.random() * 2 + 3) + 's'; // 3s to 5s
                heart.style.fontSize = (Math.random() * 1.5 + 1) + 'rem'; // 1rem to 2.5rem
                
                document.body.appendChild(heart);

                // Clean up heart after animation ends
                setTimeout(() => {
                    heart.remove();
                }, 5000);
            }, 300); // Create a new heart every 300ms
        }
    </script>
</body>
</html>
